# init-pwsh

Forgejo action source for Windows PowerShell jobs.

Usage goal:

```yaml
- uses: arachne/init-pwsh@v1
```

This directory contains the action metadata and startup script. Runtime teardown code is still pending.
