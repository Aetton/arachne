# First scenario

Create a scenario in the administration UI or import a YAML definition. A scenario
needs a unique slug, a display name, at least one step, and a driver configured for
that step.

Use the raw YAML editor for the complete definition. The form validates the document
before publishing a new immutable version.

See [Scenario syntax](/reference/scenario-syntax) for the supported fields.
