# Components

Components are reusable, database-backed building blocks used by scenarios.

Use components for fragments that must be edited, versioned, and reused independently
instead of copying YAML between scenarios. Component lifecycle and schema details will
be documented here as the model stabilizes.
